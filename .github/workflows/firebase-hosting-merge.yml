# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - ✔ Set up automatic deployment to your site's live channel when a PR is
        merged? Yes
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: i  You can manage your secrets at
          https://github.com/Marks-VB/Internship-Infos/settings/secrets.
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_INTERNSHIP_INFOS }}
          channelId: live
          projectId: internship-infos
